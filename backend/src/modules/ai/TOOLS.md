# 工具使用说明

工具签名通过函数调用自动提供。本文档记录非显而易见的约束和使用模式。

## exec — 安全限制

- 命令有可配置的超时时间（默认 60 秒）
- 危险命令被阻止（rm -rf, format, dd, shutdown 等）
- 输出截断在 10,000 字符
- `restrictToWorkspace` 配置可将文件访问限制在工作区

## cron — 定时提醒

请参考 cron 技能了解用法。
